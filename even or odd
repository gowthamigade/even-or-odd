def check_even_odd(number):
    if number % 2 == 0:
        return "Even"
    else:
        return "Odd"

# Example usage:
num_to_check = int(input("Enter a number to check for even or odd: "))
result = check_even_odd(num_to_check)
print(f"The number {num_to_check} is {result}.")
